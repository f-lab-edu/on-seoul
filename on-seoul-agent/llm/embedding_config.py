"""임베딩 파이프라인 도메인 상수.

파이프라인 전반에서 공유되는 임계값·모델명·분포 비율을 한 곳에서 관리한다.
실험 파라미터를 변경할 때 이 파일만 수정하면 된다.
"""

# ---------------------------------------------------------------------------
# detail_content 정제
# ---------------------------------------------------------------------------

#: 변별 정보 구간의 시작 마커 ("3. 상세내용" 이후부터 추출)
DETAIL_START_MARKER: str = "3. 상세내용"

#: 변별 정보 구간의 종료 마커 ("4. 주의사항" 직전까지 추출)
DETAIL_END_MARKER: str = "4. 주의사항"

# ---------------------------------------------------------------------------
# Track B — 구조화 추출
# ---------------------------------------------------------------------------

#: cleaned_detail 최소 문자 수. 이상이면 FULL 프롬프트, 미만이면 메타데이터 전용 프롬프트.
EXTRACTION_MIN_CHARS: int = 50

#: LLM 호출 실패 시 재시도 횟수 (본 호출 포함 총 시도 횟수 = MAX_RETRIES + 1).
EXTRACTION_MAX_RETRIES: int = 1

# ---------------------------------------------------------------------------
# Track C — HyQE 예상질문 생성
# ---------------------------------------------------------------------------

#: 시설당 생성할 예상질문 수 (기본값).
HYQE_QUESTIONS_PER_SERVICE: int = 6

#: 의도 유형별 목표 분포 비율.
HYQE_INTENT_DISTRIBUTION: dict[str, float] = {
    "semantic": 0.5,
    "detail": 0.3,
    "keyword": 0.2,
}

#: 분포 허용 오차 (±). 소수 개수에서 유연성 확보.
HYQE_DIST_TOLERANCE: float = 0.15
